@page
@model sadnerd.io.ATAS.OrderEventHub.Models.Pages.TopstepAccounts.IndexModel

<h1>ProjectX Accounts</h1>

<a asp-page="/TopstepAccount/Create" class="btn btn-primary mb-3">Add New Account</a>

<table class="table">
    <thead>
        <tr>
            <th>Account ID</th>
            <th>Vendor</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var account in Model.ProjectXAccounts)
        {
            <tr>
                <td>@account.ProjectXAccountId</td>
                <td>@account.Vendor</td>
                <td>
                    <a asp-page="/TopstepAccount/Delete" asp-route-id="@account.ProjectXAccountId" class="btn btn-danger">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
